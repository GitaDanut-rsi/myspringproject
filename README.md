# MySpringProject

This is a simple Spring Boot project that handles a greeting request.

## Installation

Please refer to the [installation guide](scripts/install.sh) for detailed instructions on setting up the environment and installing dependencies.

## Usage

To run the application, use the following command:

```bash
mvn spring-boot:run